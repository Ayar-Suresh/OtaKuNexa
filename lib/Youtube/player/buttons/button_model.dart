class ActionState {
  bool smashed;
  bool trashed;
  bool spread;
  bool saved;

  ActionState({
    this.smashed = false,
    this.trashed = false,
    this.spread = false,
    this.saved = false,
  });
}
